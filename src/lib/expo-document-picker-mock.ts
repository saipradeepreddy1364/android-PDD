// Web stub for expo-document-picker — never called on web because
// PatientDetail.tsx uses the native <input type="file"> path instead.
export const getDocumentAsync = async (_opts?: any): Promise<any> => ({
  canceled: true,
  assets: [],
});

export default { getDocumentAsync };
